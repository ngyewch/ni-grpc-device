name: ni-grpc-device-server
download: 
  url_template: https://github.com/ni/grpc-device/releases/download/v${VERSION}/${NAME}-linux-glibc2_38-${ARCH}.tar.gz
outputs:
  - arch: amd64
    download:
      env:
        ARCH: x64
